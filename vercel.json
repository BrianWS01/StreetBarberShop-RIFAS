{
    "crons": [
        {
            "path": "/api/cleanup",
            "schedule": "*/10 * * * *",
            "headers": {
                "x-cron-secret": "${CRON_SECRET}"
            }
        }
    ]
}